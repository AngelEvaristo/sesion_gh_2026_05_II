name: Workflow deploy dev - Node

on:
  workflow_dispatch: {}

env:
  VERSIONNODE: 20

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      CONFIGURATION: production
    steps:
      - name: Checout repo
        uses: actions/checkout@v4

      - name: Setear version Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.VERSIONNODE }}

      - name: Restore    
        run: |
          echo "Restaurar paquetes"
          npm install -g @angular/cli
          npm install

      - name: build
        run: ng build --configuration $CONFIGURATION

      - name: Crear zip usando composite action
        uses: AngelEvaristo/sesion_gh_2026_05_II_templates/.github/actions/createzip@main
        with:
          source: dist/my-first-angular-app/browser
          output: dropnode.zip
            
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: echo "Pruebas env y vars"         
  # docker-job:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Run a docker action
  #       uses: docker://alpine:3.18
  #       with:
  #         args: echo "Hola desde Alpine dentro de github Actions"

